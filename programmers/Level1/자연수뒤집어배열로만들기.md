## 자연수 뒤집어 배열로 만들기

<hr/>

### :heavy_check_mark: 내가 푼 답안

```javascript
function solution(n) {
  return [...(n + '')].reverse().map(e => parseInt(e));
}
```

배열로 만든어 준 후 reverse함수를 통해 배열을 반대로 만들어줬다.
여기서 끝나면 string[] 이 되니까 int[] 로 바꿔줘야한다.  
<br/>
